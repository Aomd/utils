declare function formArray(arr: []): [];
declare function max(arr: []): num;
declare function min(arr: []): num;
declare function sum(arr: []): num;

export {
    formArray,
    max,
    min,
    sum
}